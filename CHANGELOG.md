# Changelog

## v1.2.0 – 2026-09-05

- Added **build artifact pruning** script (`scripts/prune-build-artifacts.mjs`).
- Integrated pruning into `npm run build` pipeline.
- Added CI workflow (`.github/workflows/prune-artifacts.yml`) to run nightly and on pushes.
- Added automated test `tests/prune-build-artifacts.test.ts`.
- Updated README with pruning section and FAQ link.
- Created FAQ entry for pruning.
- Created GitHub Release tag `v1.2.0`.
- Updated `package.json` version to `1.2.0`.
- Updated documentation and workflow files.

## v1.1.0 – 2026-08-20

- Initial release with core language‑learning features.
