// Add reviewed version-1 plans keyed by pack ID. Empty means authoring is outstanding,
// not that the legacy word decks satisfy daily vocabulary progression.
export const DAILY_PLANS = Object.freeze({});
